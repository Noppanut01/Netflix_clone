import 'package:flutter/material.dart';

class MyNetflixScreen extends StatelessWidget {
  const MyNetflixScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Hello My netflix screen"),
    );
  }
}
